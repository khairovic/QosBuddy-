# QoSBuddy M6 — DSO3.2 Requirements
# PingWin · ESPRIT 2025-2026

# ── Core data ──────────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.0          # xlsx reading
pypdf>=4.0.0             # PDF text extraction for domain context docs

# ── Embeddings & Vector DB ─────────────────────────────────────────────────────
sentence-transformers>=3.0.0   # all-MiniLM-L6-v2 + cross-encoder reranking
chromadb>=0.5.0                # persistent local vector store

# ── LangChain RAG stack ────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0        # Ollama integration (llama3.2)

# ── Dashboard ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0

# ── API ────────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
requests>=2.31.0               # streaming RAG endpoint calls Ollama directly

# ── M6 AI Improvements ─────────────────────────────────────────────────────────
rank-bm25>=0.2.2               # Improvement #1: BM25 sparse retrieval
scikit-learn>=1.4.0            # Improvement #5: IsolationForest for SHAP
shap>=0.44.0                   # Improvement #5: SHAP explainability (XAI)

# ── RL Agent (DSO3.1 — PPO policy on NS-3 digital twin) ───────────────────────
stable-baselines3>=2.3.0
gymnasium>=0.29.0
torch>=2.2.0              # SB3 backend; CPU-only inference is sufficient

# ── M1 anomaly ensemble + M2 forecasts + M3 causal (live inference) ──────────
xgboost>=2.0.0            # M2 jitter/throughput + M1 supervised anomaly
joblib>=1.3.0             # Loads M1 IF/RF/GBT pickle bundles
dowhy>=0.12               # M3 live counterfactual estimator
prophet>=1.1.5            # M2 forecast regeneration (offline)
causal-learn>=0.1.4       # DoWhy dependency for graph discovery

# ── PDF export ─────────────────────────────────────────────────────────────────
reportlab>=4.2.0

# ── Dev / testing ──────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0              # for FastAPI TestClient